[package]
name = "turboshreds-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
turboshreds-sdk = { git = "https://github.com/turboshreds/turboshreds-sdk", tag = "v0.1.2" }
bs58 = "0.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
